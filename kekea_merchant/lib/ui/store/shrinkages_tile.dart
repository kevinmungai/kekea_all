import 'package:flutter/material.dart';

class ShrinkagesTile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text("Shrinkage"),
    );
  }
}
